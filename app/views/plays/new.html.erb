<%= form_for(@play) do |f| %>
  <% if @play.errors.any? %>

    <div id='errors'>
      <% @play.errors.full_messages.each do |message| %>
        <%= message %>
      <% end %>
    </div>

  <% end %>

  <%= f.label :word %>
  <%= f.text_field :word %>

  <%= f.submit 'Play!' %>

<% end %>
